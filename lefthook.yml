pre-commit:
  parallel: true
  commands:
    fmt:
      glob: "*.rs"
      run: cargo fmt --check
    clippy:
      glob: "*.rs"
      run: cargo clippy -- -D warnings

pre-push:
  commands:
    coverage:
      run: cargo tarpaulin --config tarpaulin.toml --skip-clean
